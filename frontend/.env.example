# Copy this to .env.production and fill in your backend's real URL once
# it's hosted somewhere (Railway, Render, Fly.io, a VPS, etc). Leave it
# unset for local dev — Vite's proxy in vite.config.js handles that.
VITE_API_BASE=https://api.gridline.wiki
